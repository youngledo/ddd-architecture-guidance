interface:
  display_name: "Domain Modeling"
  short_description: "Model business domains before implementation"
  default_prompt: "Use $domain-modeling to produce a scoped strategic and/or tactical Domain Modeling Result. For substantive applicable modeling, include item-level evidence and explicit deferred areas; for simple CRUD or a pure read change with no modeling decision, return a compact not-applicable result without expanding the payload."
